# ===== VARIABLES =====
# Colors
$background = rgb(0a0a0a)
$backgroundFull = rgba(0a0a0aff)
$white = rgba(ffffffff)
$gray = rgba(c0c0c0ff)
$grayAlpha = rgba(80808088)
$darkGray = rgba(606060ff)
$lightGray = rgba(ccccccff)

# Typography
$font = Agave Nerd Font
$timeFontSize = 80
$dateFontSize = 22

# Layout positions
$timePositionX = 10
$timePositionY = -10
$datePositionY = 10
$inputPositionY = 150

# Input field
$inputWidth = 300
$inputHeight = 50
$outlineThickness = 2
$dotsSize = 0.25
$dotsSpacing = 0.3

# Image
$imagePath = ~/.config/backgrounds/base16-default-dark.png

# ===== GENERAL =====
general {
    hide_cursor = true
}

# ===== BACKGROUND WIDGET =====
background {
    monitor =
    path = $imagePath
}

# ===== TIME LABEL WIDGET =====
label {
    monitor =
    text = $TIME
    color = $white
    font_size = $timeFontSize
    font_family = $font
    position = $timePositionX, $timePositionY
    halign = left
    valign = top
}

# # ===== DATE LABEL WIDGET =====
# label {
#     monitor = 
#     text = cmd[update:43200000] echo "$(date +'%A, %B %d')"
#     color = $gray
#     font_size = $dateFontSize
#     font_family = $font
#     position = 0, $datePositionY
#     halign = center
#     valign = center
# }

# ===== INPUT FIELD WIDGET =====
input-field {
    monitor =
    size = $inputWidth, $inputHeight
    outline_thickness = $outlineThickness
    dots_size = $dotsSize
    dots_spacing = $dotsSpacing
    dots_center = true
    outer_color = $white
    inner_color = $backgroundFull
    font_color = $white
    placeholder_text = <span foreground="#ffffff"></span>
    hide_input = false
    fail_color = rgb(ff4444)
    fail_text = failed
    check_color = $white
    capslock_color = $lightGray
    position = 0, $inputPositionY
    halign = center
    valign = bottom
}
